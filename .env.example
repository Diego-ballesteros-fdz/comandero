# Vercel KV (Redis) — requerido en producción para persistir el menú
# Crear en: Vercel Dashboard → Storage → Create → KV
# En local puedes omitirlo; se usará data/platos.txt como fallback
KV_REST_API_URL=
KV_REST_API_TOKEN=
